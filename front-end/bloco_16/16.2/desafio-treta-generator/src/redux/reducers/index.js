// Reducers

const INITIAL_STATE = {
  test: 'funcionou'
};

export const tretaReducer = (state = INITIAL_STATE, action) => ({
  test: state
})


 // switch (action.type) {
  //   case 'ADD_NAME':
      
  //     break;
  
  //   default:
  //     break;
  // }
